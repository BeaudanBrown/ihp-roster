---
id: ir-9t94
status: closed
deps: [ir-myjr]
links: []
created: 2026-05-29T03:16:09Z
type: task
priority: 2
assignee: beaudan
parent: ir-oxnj
tags: [agent-loop, xero, containment, frontend-surface]
---
# Verify Xero containment and semantic fragment selection

Prepare Xero fragments for semantic actor-local and websocket invalidation without business OOB response composition.

## Design

Define/verify containment paths and fragment selection for shell, staff mappings, pay items, and timesheets target nodes. Keep fragment GETs as exact target-node renderers. Use containment/dependency planning to avoid redundant shell+child invalidations rather than composing duplicate OOB HTML.

## Acceptance Criteria

Live surface contract tests cover Xero target ids, URLs, dependencies, and containment. Semantic invalidation planning avoids duplicate parent/child refreshes where appropriate. Plain fragment GETs remain exact target nodes.

## Notes

**2026-07-07T04:28:23Z**

Verified Admin Xero fragment contract and dependency planning. Added SurfaceDependencySpec coverage for mounted Xero wire fragment keys/target ids/URLs and for resource-to-fragment planning: xeroConnection -> shell, xeroMappings -> staff mappings, xeroPayItems -> pay items, xeroTimesheets -> timesheets. Current model intentionally treats shell and child fragments as separate semantic fragments; planning does not add shell for child-only resources, avoiding shell+child duplicate refreshes unless a mutation emits both resources explicitly. Fragment GET actions remain the plain target-node endpoints referenced by the mounted wire fragments. Verification: bash ./bin/in-env hspec-test --match 'generated FrontendSurface resource dependencies'.
