# Archive

Archived docs are historical context. They are useful for understanding why
work happened, but they are not current implementation guidance unless an
active ticket or workstream links to them.

Current behavior belongs in subsystem-local `SPEC.md` files. Current agent
rules belong in local `AGENTS.md` files. Live work status belongs in `.tickets/`.
