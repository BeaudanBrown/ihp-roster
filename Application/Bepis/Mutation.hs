module Application.Bepis.Mutation () where

-- Final Bepis mutation semantics are runtime facts emitted by effect helpers.
-- The former descriptive mutation metadata APIs were intentionally removed; use
-- runBepis plus fact-emitting authorization, audit, live, and response helpers
-- instead.
