#!/usr/bin/env bash
set -euo pipefail

# Retain the historic public standalone composite. verify-full calls only the
# repository authority atom; the synthetic sanitizer fixture belongs to tooling.
echo "billing-production-readiness: repository contracts"
billing-contract-check

echo "billing-production-readiness: Stripe Sandbox probe sanitizer"
stripe-sandbox-contract-parity --self-test

echo "Billing production-readiness checks passed"
