#!/usr/bin/env bash
set -euo pipefail

# Compatibility alias. Canonical production package inspection owns filtered
# source, inventory, executable, dependency, and runtime-closure assertions.
exec production-package-smoke "$@"
